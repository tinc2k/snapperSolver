@model List<string>

@{
    Layout = null;
}

@if (Model.Count > 0){
    <span>There are @Model.Count solutions:</span>
    <ul>
        @foreach (var solution in Model)
        {
            <li>@solution</li>    
        }
    </ul>
} else {
    <span>There are no solutions.</span>
}